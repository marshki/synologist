#!/usr/bin/env bash
# Extract Etherent device assignment; MAC address; IP address. 

# Iterate through Ethernet devices (0-5) using ip addr command
# awk to match eth and extract column contents

for i in $(seq -w 0 5 $END);
  do ip addr show eth$i|awk '/eth/ {print $2}';

done
